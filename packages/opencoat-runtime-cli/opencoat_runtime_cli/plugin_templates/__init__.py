"""Starter file templates for ``COATr plugin install <host>`` (post-M5 DX).

Each sub-package (``openclaw``, ``custom``) ships ``bootstrap_opencoat.py`` +
``host_adapter.py`` + ``concerns.py`` as valid, lint-clean Python files
that the CLI copies verbatim into a user's target directory.
"""
