[project]
name = "COAT-runtime-host-plugins"
version = "0.0.1"
description = "First-party host adapters for the COAT Runtime: openclaw, hermes, langgraph, autogen, crewai, custom."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }
authors = [{ name = "HyperdustLab" }]
dependencies = [
  "COAT-runtime-protocol",
  "COAT-runtime-core",
  "COAT-runtime-host-sdk",
]

[project.optional-dependencies]
openclaw  = []
hermes    = []
langgraph = ["langgraph>=0.2"]
autogen   = []
crewai    = []

[build-system]
requires = ["hatchling>=1.24"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = [
  "COAT_runtime_host_openclaw",
  "COAT_runtime_host_hermes",
  "COAT_runtime_host_langgraph",
  "COAT_runtime_host_autogen",
  "COAT_runtime_host_crewai",
  "COAT_runtime_host_custom",
]
