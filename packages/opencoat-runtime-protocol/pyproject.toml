[project]
name = "opencoat-runtime-protocol"
version = "0.0.1"
description = "JSON Schemas + pydantic envelopes for the OpenCOAT Runtime data contracts"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }
authors = [{ name = "HyperdustLabs" }]
dependencies = [
  "pydantic>=2.7",
  "jsonschema>=4.21",
]

[build-system]
requires = ["hatchling>=1.24"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["opencoat_runtime_protocol"]

[tool.hatch.build.targets.wheel.sources]
"opencoat_runtime_protocol" = "opencoat_runtime_protocol"

[tool.hatch.build.targets.wheel.force-include]
"opencoat_runtime_protocol/schemas" = "opencoat_runtime_protocol/schemas"
"opencoat_runtime_protocol/openapi" = "opencoat_runtime_protocol/openapi"
