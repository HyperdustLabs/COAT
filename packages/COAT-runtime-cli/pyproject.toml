[project]
name = "COAT-runtime-cli"
version = "0.0.1"
description = "Open COAT Runtime — COATr command-line interface."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }
authors = [{ name = "HyperdustLabs" }]
dependencies = [
  "COAT-runtime-protocol",
  "COAT-runtime-core",
  "COAT-runtime-storage",
  "COAT-runtime-daemon",
  "pyyaml>=6.0",
]

[project.scripts]
COATr = "COAT_runtime_cli.main:main"

[build-system]
requires = ["hatchling>=1.24"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["COAT_runtime_cli"]
