[project]
name = "opencoat-runtime-llm"
version = "0.0.1"
description = "LLM and Embedder clients for the OpenCOAT Runtime (openai, anthropic, azure, ollama, stub)."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }
authors = [{ name = "HyperdustLabs" }]
dependencies = [
  "opencoat-runtime-protocol",
  "opencoat-runtime-core",
]

[project.optional-dependencies]
openai    = ["openai>=1.40"]
anthropic = ["anthropic>=0.30"]
azure     = ["openai>=1.40"]
ollama    = ["httpx>=0.27"]

[build-system]
requires = ["hatchling>=1.24"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["opencoat_runtime_llm"]
